jacoco {
  toolVersion = '0.8.11'
}

tasks.named("jacocoTestReport").configure {
  group = "Reporting"
  description = "Generates JaCoCo coverage (XML/HTML) for tests."

  reports {
    xml.required = true
    html.required = true
    csv.required = false
  }
}
